{
  "name": "rockpaperscissors",
  "version": "1.0.0",
  "description": "Rock Paper Scissors PWA",
  "scripts": {
    "start:dev": "cd client && npm run dev",
    "start": "npm run build",
    "build": "cd client && npm run build",
    "install": "cd client && npm i"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}
